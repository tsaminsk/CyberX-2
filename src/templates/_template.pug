block variables

doctype html
html(lang="ru")
    head
        meta(charset="utf-8")
        title #{title}
        meta(content="Matvey Phoenix" name="author")
        meta(content="" name="description")
        meta(content="width=device-width, initial-scale=1" name="viewport")
        meta(content="ie=edge" http-equiv="x-ua-compatible")
        //- script(src="https://api-maps.yandex.ru/2.1/?load=package.standard&amp;lang=ru-RU")
        script(src=scriptLink)
        link(type='image/x-icon', rel='shortcut icon', href='./assets/images/favicon.ico')
        link(rel='stylesheet' href='./assets/styles/main.min.css')

    body
        include ./vars/_var
        include ./vars/_crumbs-vars
        include ./mixins/_mixins
        
        +headerBlock(topMenu, pageUrl, headerClass)

        block content

        include ./modules/_footer

        script(src="./assets/scripts/libs.min.js")
        script(src="./assets/scripts/script.min.js")